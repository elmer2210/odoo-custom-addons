<odoo>
    <!-- Grupo: Administrador -->
     <!-- Grupo: Administrador -->
    <record id="group_university_admin" model="res.groups">
        <field name="name">Administrador</field>
        <field name="category_id" ref="base.module_category_university"/>
        <field name="users" eval="[(4, ref('base.user_admin'))]"/>
    </record>

    <!-- Grupo: Bibliotecario -->
    <record id="group_librarian" model="res.groups">
        <field name="name">Bibliotecario</field>
        <field name="category_id" ref="base.module_category_university"/>
    </record>
</odoo>
