<odoo>
  <!-- Añadimos la pestaña CRAI al formulario de usuario para asignar sedes -->
  <record id="view_users_form_inherit_crai" model="ir.ui.view">
    <field name="name">res.users.form.inherit.crai</field>
    <field name="model">res.users</field>
    <field name="inherit_id" ref="base.view_users_form"/>
    <field name="arch" type="xml">
      <notebook position="inside">
        <page string="CRAI">
          <group>
            <field name="crai_campus_ids" widget="many2many_tags"
                   placeholder="Selecciona las sedes que este usuario gestionará"/>
          </group>
        </page>
      </notebook>
    </field>
  </record>

  <!-- Opcional: acción y menú directo para Admin si quieres acceder rápido a Usuarios -->
  <record id="action_res_users_crai_admin" model="ir.actions.act_window">
    <field name="name">Usuarios</field>
    <field name="res_model">res.users</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{'form_view_ref': 'base.view_users_form'}</field>
  </record>

  <menuitem id="menu_crai_users" name="Usuarios" parent="menu_crai_config"
            action="action_res_users_crai_admin" sequence="99"
            groups="crai_base.group_crai_admin"/>
</odoo>
